{
  "name": "fcclubs-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "prebuild": "npm test"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
